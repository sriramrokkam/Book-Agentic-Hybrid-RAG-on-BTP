# cap-srv/.env.example
# Copy to .env before running `cds serve` locally.

# URL of the running Python FastAPI agent service
# Local development:
AGENT_URL=http://localhost:8000
# BTP Cloud Foundry (set after cf push):
# AGENT_URL=https://hybrid-rag-agent.<your-cf-domain>
